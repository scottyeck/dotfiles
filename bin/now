#!/bin/bash

# Constants
date="date"

shownow () {
  date -u +"%Y-%m-%dT%H:%M:%SZ"
}

showdate () {
  date -u +"%Y-%m-%d"
}

now () {
  local cmd=$1
  if [ "$cmd" = "$date" ] ; then
    showdate "$@"
  else
    shownow "$@"
  fi
}

now "$@"
